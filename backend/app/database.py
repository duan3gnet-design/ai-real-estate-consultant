# NOTE: SQLite đã được thay thế hoàn toàn bằng Elasticsearch.
# Xem app/es_client.py cho client + index initialization,
# và app/embedding.py cho sentence-transformer embedding service.
#
# File này được giữ lại (rỗng) để tránh ImportError ở các module cũ
# nếu còn sót lại reference, nhưng không nên import gì từ đây nữa.
